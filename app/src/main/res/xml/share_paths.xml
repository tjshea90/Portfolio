<?xml version="1.0" encoding="utf-8"?>
<!--
  What the FileProvider in AndroidManifest.xml may hand out: the prompt files staged for the
  share sheet (PromptShare.stage), and nothing else in the app's storage.
-->
<paths>
    <cache-path name="shared" path="shared/" />
</paths>
